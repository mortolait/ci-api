name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    name: 'Build and push'
    runs-on: ubuntu-latest

    steps:
    # Ter acesso ao codigo (git checkout)
    - uses: actions/checkout@v4
